{
  "name": "inmobo-scripts",
  "version": "1.0.0",
  "description": "One-off migration and maintenance scripts for the Inmobo platform",
  "type": "module",
  "private": true,
  "scripts": {
    "migrate:resources:dry": "node --env-file=.env migrate-resources.js",
    "migrate:resources:live": "DRY_RUN=false node --env-file=.env migrate-resources.js"
  },
  "dependencies": {
    "node-appwrite": "^17.0.0"
  }
}
